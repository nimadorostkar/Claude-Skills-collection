# Assets

Images, diagrams, and other binary artifacts referenced by the documentation.

Skills themselves are text-only by design: an image cannot be loaded into an agent's context usefully, and a skill that depends on one is a skill that does not work. Anything visual a skill needs to convey belongs in a Mermaid diagram or an ASCII figure inside the `SKILL.md`.
